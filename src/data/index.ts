/* Barrel for sample seed data. Centralised so it can be swapped for live
   queries (Firebase / Supabase / custom API) later. */
export { CURRENT_MONTH } from './month';
export { BRANCHES, BRANCH_NAMES } from './branches';
export { EMPLOYEES } from './employees';
export { FORMULA_BLOCKS, BLOCK_TYPE_META } from './formulaBlocks';
export { NOTICES } from './notices';
